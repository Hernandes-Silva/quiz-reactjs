// @flow 
import * as React from 'react';
type Props = {
    
};

const Login = (props: Props) => {
    
    return (
        <div>
            <p>Login</p>
        </div>
    );
};

export default Login;